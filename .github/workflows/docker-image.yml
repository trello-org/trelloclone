name: Docker Image CI

on:
  push:
    branches: [ docker-final ]
  pull_request:
    branches: [ docker-final ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file ./TrelloClone/Dockerfile --tag my-image-name:$(date +%s)
